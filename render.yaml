services:
  - type: web
    name: smartwaste-ai
    env: python
    runtime: python
    pythonVersion: "3.10.12"
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.12"